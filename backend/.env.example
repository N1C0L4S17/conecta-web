# PostgreSQL (Neon). Formato: postgresql://user:pass@host/db?sslmode=require
DATABASE_URL="postgresql://user:password@ep-xxx.neon.tech/conecta?sslmode=require"

# JWT
JWT_SECRET="cambia-esto-por-un-secreto-largo-y-aleatorio"
JWT_EXPIRES_IN="8h"

# Servidor
PORT=4000
# Origen permitido del frontend (Vercel)
CORS_ORIGIN="http://localhost:3000"
